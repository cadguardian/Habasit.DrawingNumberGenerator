<div class="instructions small">
    <p>
        The Drawing Request Extraction service helps streamline the process of extracting specific drawing request data from email content. By automating this extraction, the service ensures that important information, such as belt type, material, and other attributes, are captured accurately and efficiently.
    </p>

    <h2>Instructions</h2>
    <ol>
        <li><span style="background-color: yellow;">Paste the email content</span> that contains the drawing request information into the provided text area.</li>
        <li><span style="background-color: yellow;">Click the "Extract Data" button</span> to initiate the process of extracting relevant information from the email.</li>
        <li><span style="background-color: yellow;">Review the extracted drawing request</span> that appears below the form. The extracted data will be displayed in a clear and structured format.</li>
        <li><span style="background-color: yellow;">If any errors occur</span> during extraction, an error message will appear. Review the message and correct any issues with the email content.</li>
    </ol>

    <h3>Data Extraction Speed and Efficiency Tips</h3>
    <ul>
        <li><strong>Clear Email Formatting:</strong> Ensure the email content is in a simple format, without unnecessary formatting, to help the extraction process.</li>
        <li><strong>Focus on Relevant Information:</strong> Try to structure the email with the relevant data (e.g., drawing number, material, belt type) clearly visible to aid extraction accuracy.</li>
        <li><strong>Quick Email Copying:</strong> Use <kbd>Ctrl</kbd> + <kbd>A</kbd> to select all email content, and <kbd>Ctrl</kbd> + <kbd>C</kbd> to copy it into the input field.</li>
        <li><strong>Paste for Extraction:</strong> After pasting, you can directly click the "Extract Data" button or press <kbd>Enter</kbd> to trigger the extraction.</li>
        <li><strong>Email Error Handling:</strong> If the extraction doesn’t yield the correct result, review the email content for missing or unclear fields. Correct the data format as needed.</li>
        <li><strong>Re-Submit if Needed:</strong> If the extraction is not accurate, you can edit the email content and click "Extract Data" again to retry.</li>
    </ul>
</div>
